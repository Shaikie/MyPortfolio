/* Graphite Field Notes: lab page treats unfinished work as documented investigations, not fabricated achievements. */
import { ArrowUpRight, FlaskConical } from "lucide-react";
import { labNotes } from "@/lib/portfolio";
export default function Lab() { return <div className="page interior-page lab-page"><div className="page-intro"><p className="eyebrow"><span className="signal-bar" /> 04 / EXPERIMENT LOG</p><h1>Where the<br /><em>questions live.</em></h1><p className="intro-copy">A lab is not a showcase of finished answers. It is a record of the hypotheses, prototypes, scripts, and investigations that make the next build sharper.</p></div><div className="lab-board section-rule"><div className="lab-board-head"><span className="section-index">LOG / 2026</span><span className="micro-label"><FlaskConical size={14} /> ACTIVE NOTEBOOK</span></div>{labNotes.map(([num, title, copy]) => <article className="lab-entry" key={num}><span>{num}</span><div><h2>{title}</h2><p>{copy}</p></div><ArrowUpRight size={17} /></article>)}</div><div className="placeholder-note wide"><span className="status-dot" /><span><strong>Lab notes need source material.</strong><br />Add links, dates, screenshots, and short write-ups as experiments become publishable.</span></div></div> }
